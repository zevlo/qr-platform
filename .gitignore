# Environment
.env
.env.*
!.env.example
api/.env
front-end-nextjs/.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Node
node_modules/
.next/
out/
.npm/
.pnp.*

# Terraform
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
*.tfvars
!*.tfvars.example
crash.log
crash.*.log

# NOTE: .terraform.lock.hcl above is gitignored for this single-operator repo.
# Per Hashicorp's guidance it should normally be committed, but since there's
# only one operator and we want `terraform init` to always pull fresh hashes,
# leaving it ignored is the lower-friction choice. Re-enable if collaborators join.

# Kubernetes
kubeconfig*
*.kube
.kube/

# AWS
.aws/
*.pem
*.key

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Build output
dist/
build/
